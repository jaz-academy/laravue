on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.0
      with:
        server: ftp.jazacademy.id
        username: abukafa@jazacademy.id
        password: nM~[VFWppgAiLoVy
        protocol: ftp
        server-dir: /laravue
